---
title: ''
originalTitle: '' # opcional
director: ''
year: 0
country: '' # opcional
runtime: 0 # opcional, minutos
thread: '' # el hilo del archivo que la trae (mismo texto agrupa; bilingüe por archivo)
why: '' # por qué verla, en una o dos frases, sin spoilers
tips:
  - '' # claves para el visionado, sin spoilers
seeds: [] # slugs neutrales de películas del archivo que la sugieren (ej. ['heat', 'vertigo'])
tags: []
order: 0 # orden curado dentro de la página
draft: true
---

<!--
  La watchlist no tiene cuerpo: todo vive en el frontmatter (why + tips).
  Recordá crear el archivo en los DOS idiomas: en/<slug>.md y es/<slug>.md,
  con el MISMO nombre de archivo (slug neutral) y los mismos `seeds`/`order`.
  Poné draft: false cuando esté lista y validá con `npm run build`.
-->
